---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oa2pwhoami-cert
  namespace: keycloak
spec:
  issuerRef:
    name: letsencrypt-issuer-prod
    kind: ClusterIssuer
  dnsNames:
    - oa2pwhoami.keycloak.kubernasty.micahrl.com
  secretName: oa2pwhoami-cert-backing-secret
