---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oauth2-proxy-cert
  namespace: keycloak
spec:
  issuerRef:
    name: letsencrypt-issuer-prod
    kind: ClusterIssuer
  dnsNames:
    - oauth2-proxy.kubernasty.micahrl.com
  secretName: oauth2-proxy-cert-backing-secret
