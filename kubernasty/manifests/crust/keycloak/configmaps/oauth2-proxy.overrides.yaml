apiVersion: v1
kind: ConfigMap
metadata:
  name: oauth2-proxy-value-overrides
  namespace: keycloak
data:
  values.yaml: |-
    config:
      existingSecret: oauth2-proxy-secrets
      extraArgs:
        - "--reverse-proxy"
      httpScheme: http
      containerPort: 80
