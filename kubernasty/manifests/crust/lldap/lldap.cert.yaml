---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lldap-cert
  namespace: lldap
spec:
  issuerRef:
    name: letsencrypt-issuer-prod
    kind: ClusterIssuer
  dnsNames:
    - lldap.kubernasty.micahrl.com
  secretName: lldap-cert-backing-secret
