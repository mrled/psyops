apiVersion: v1
kind: Service
metadata:
  name: openldap
  namespace: openldap
  labels:
    app.kubernetes.io/name: openldap
spec:
  type: ClusterIP
  ports:
    - name: tcp-ldap-privileged
      port: 389
      targetPort: 1389
    - name: tcp-ldaps-privileged
      port: 689
      targetPort: 1689
  selector:
    app.kubernetes.io/name: openldap
