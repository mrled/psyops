apiVersion: v1
kind: Service
metadata:
  name: phpldapadmin
  namespace: openldap
  labels:
    app.kubernetes.io/name: phpldapadmin
spec:
  type: ClusterIP
  ports:
    - name: phpldapweb
      port: 80
  selector:
    app.kubernetes.io/name: phpldapadmin
