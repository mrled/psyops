apiVersion: v1
kind: Service
metadata:
  name: ldap-account-manager
  namespace: openldap
  labels:
    app.kubernetes.io/name: ldap-account-manager
spec:
  type: ClusterIP
  ports:
    - name: ldap-account-manager
      port: 80
      targetPort: lam-web
  selector:
    app.kubernetes.io/name: ldap-account-manager
