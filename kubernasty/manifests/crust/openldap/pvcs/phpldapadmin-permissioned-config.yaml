kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: phpldapadmin-permissioned-config-claim
  namespace: openldap
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Mi
  storageClassName: local-path
