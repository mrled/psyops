apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: longhorn.${clusterTld}
  namespace: external-dns
spec:
  endpoints:
  - dnsName: longhorn.${clusterTld}
    recordTTL: 60
    recordType: CNAME
    targets:
    - ${clusterPrimaryDns}
