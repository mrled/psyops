apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: treasury.${clusterTld}
  namespace: external-dns
spec:
  endpoints:
  - dnsName: treasury.${clusterTld}
    recordTTL: 60
    recordType: CNAME
    targets:
    - ${clusterPrimaryDns}
