apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kubernetes-dashboard-cert
spec:
  issuerRef:
    name: letsencrypt-issuer-prod
    kind: ClusterIssuer
  dnsNames:
    - dashboard.kubernasty.micahrl.com
  secretName: kubernetes-dashboard-cert-backing-secret
