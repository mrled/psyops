apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: macrofactor
  namespace: datadump
spec:
  service:
    ports:
      - name: http
        port: 12000
        targetPort: 12000
  webhook:
    macrofactor-webhook:
      endpoint: /webhook
      method: POST
      port: 12000
