apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: labnotes-deploy
  namespace: argowf
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: cephalopodblk-nvme-2rep
  volumeName: labnotes-deploy
  resources:
    requests:
      storage: 1Gi
