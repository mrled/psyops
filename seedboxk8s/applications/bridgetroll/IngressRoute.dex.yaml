apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dex
  namespace: bridgetroll
  annotations:
    kubernetes.io/ingress.class: traefik-tailscale
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`dex.${clusterFqdn}`)
    kind: Rule
    services:
    - name: dex
      port: 5556
  tls:
    secretName: younix-dot-us-wildcard-backing-secret
