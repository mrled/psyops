# Client secrets for external services are stored here.
# (Client secrest for in-cluster services are stored in their own secret manifest
# which can be shared with the service that needs it.)
kind: Secret
apiVersion: v1
type: Opaque
metadata:
    name: dex-client-secrets
    namespace: bridgetroll
stringData:
    # Must match what's in the seedboxk8s progfiguration role for the API server OIDC client secret.
    dexDeedboxk8sKubernetesApiClientSecret: oe5eeyaePh8jaiphiChohSeeh8roongi