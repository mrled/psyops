apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seedboxmedia-nfs-pvc
  namespace: sabnzbd
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 512Mi
  volumeName: seedboxmedia-nfs-pv
