apiVersion: batch/v1
kind: CronJob
metadata:
  name: restic-backup
  namespace: restic-backup
  labels:
    app.kubernetes.io/name: kvrb
    app.kubernetes.io/component: controller
spec:
  # Keep suspended until the secret values are filled and a manual run succeeds.
  suspend: true
  schedule: "0 5 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    metadata:
      labels:
        app.kubernetes.io/name: kvrb
        app.kubernetes.io/component: controller
    spec:
      backoffLimit: 0
      template:
        metadata:
          labels:
            app.kubernetes.io/name: kvrb
            app.kubernetes.io/component: controller
        spec:
          restartPolicy: Never
          serviceAccountName: restic-backup
          containers:
            - name: backup
              image: alpine:3.22
              imagePullPolicy: IfNotPresent
              envFrom:
                - secretRef:
                    name: restic-backup-env
              env:
                - name: AWS_DEFAULT_REGION
                  valueFrom:
                    configMapKeyRef:
                      name: restic-backup-config
                      key: AWS_DEFAULT_REGION
                - name: RESTIC_REPOSITORY_BASE
                  valueFrom:
                    configMapKeyRef:
                      name: restic-backup-config
                      key: RESTIC_REPOSITORY_BASE
                - name: BACKUP_PARALLELISM
                  valueFrom:
                    configMapKeyRef:
                      name: restic-backup-config
                      key: BACKUP_PARALLELISM
              command:
                - /bin/sh
                - -ceu
              args:
                - |
                  apk add --no-cache ca-certificates kubectl python3
                  PYTHONPATH=/opt python3 -m kvrb
              volumeMounts:
                - name: kvrb-module
                  mountPath: /opt/kvrb
                  readOnly: true
          volumes:
            - name: kvrb-module
              configMap:
                name: kvrb-module
                defaultMode: 0555
