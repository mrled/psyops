apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opensearch
  namespace: inelastic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-slow-storage
  resources:
    requests:
      storage: 10Gi
