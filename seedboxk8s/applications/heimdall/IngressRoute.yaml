apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: heimdall
  namespace: tortuga
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`heimdall.${clusterFqdn}`)
      kind: Rule
      services:
        - name: heimdall
          kind: Service
          port: 80
      middlewares:
        - name: bridgetroll-mw
          namespace: bridgetroll
  tls:
    secretName: younix-dot-us-wildcard-backing-secret
