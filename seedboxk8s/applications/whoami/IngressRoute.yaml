apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami-ingress-route
  namespace: whoami
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`whoami.${clusterTld}`)
      kind: Rule
      services:
        - name: whoami-service
          kind: Service
          port: 80
  tls:
    secretName: younix-dot-us-wildcard-backing-secret
