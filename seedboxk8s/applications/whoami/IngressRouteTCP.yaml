apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: whoami-ingresstcp-route
  namespace: whoami
spec:
  entryPoints:
    - whoami-http-tcp
  routes:
    - services:
        - name: whoami-service
          kind: Service
          port: 80
