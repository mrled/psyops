apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: whoami-ingresstcp-route
  namespace: whoami
spec:
  entryPoints:
    - whoami-http-tcp
  routes:
    - match: Host(`*`)
      services:
        - name: whoami-service
          port: 80
