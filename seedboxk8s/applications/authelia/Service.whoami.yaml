apiVersion: v1
kind: Service
metadata:
  name: whoami-authelia
  namespace: authelia
spec:
  selector:
    app.kubernetes.io/name: whoami-authelia
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
