apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr
  namespace: tortuga
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`radarr.${clusterFqdn}`)
      kind: Rule
      services:
        - name: radarr
          kind: Service
          port: 80
      middlewares:
        - name: bridgetroll-mw
          namespace: bridgetroll
  tls:
    secretName: younix-dot-us-wildcard-backing-secret
