apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bk2-metube-state-pvc
  namespace: tortuga
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: seedbox-slow-mirror
  resources:
    requests:
      storage: 10Gi
