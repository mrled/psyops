apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-pvc
  namespace: sonarr
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-slow-storage
  resources:
    requests:
      storage: 512Mi
