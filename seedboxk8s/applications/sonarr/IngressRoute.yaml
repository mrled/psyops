apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr
  namespace: tortuga
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr.${clusterFqdn}`)
      kind: Rule
      services:
        - name: sonarr
          kind: Service
          port: 80
      middlewares:
        - name: bridgetroll-mw
          namespace: bridgetroll
  tls:
    secretName: younix-dot-us-wildcard-backing-secret
