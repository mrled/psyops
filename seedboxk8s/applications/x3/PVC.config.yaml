apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: x3-config-pvc
  namespace: tortuga
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: seedbox-slow-mirror
  resources:
    requests:
      storage: 1Gi
