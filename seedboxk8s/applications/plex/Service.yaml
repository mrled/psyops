apiVersion: v1
kind: Service
metadata:
  name: plex
  namespace: tortuga
spec:
  type: ClusterIP
  selector:
    app: plex
  ports:
  - port: 32400
    targetPort: 32400
    name: plex
    protocol: TCP
