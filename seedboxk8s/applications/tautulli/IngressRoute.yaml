apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: tautulli
  namespace: tortuga
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`tautulli.${clusterTld}`) || Host(`seedbox.${clusterTld}`)
      kind: Rule
      services:
        - name: tautulli
          kind: Service
          port: 80
      middlewares:
        - name: bridgetroll-mw
          namespace: bridgetroll
  tls:
    secretName: younix-dot-us-wildcard-backing-secret
