apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nzbhydra2-config-pvc
  namespace: tortuga
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: seedbox-slow-mirror
  resources:
    requests:
      storage: 1Gi
