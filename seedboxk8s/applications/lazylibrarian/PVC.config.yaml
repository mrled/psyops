apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lazylibrarian-config-pvc
  namespace: tortuga
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: seedbox-slow-mirror
  resources:
    requests:
      storage: 1Gi
