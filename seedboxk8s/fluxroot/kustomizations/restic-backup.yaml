apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: restic-backup
  namespace: flux-system
spec:
  interval: 15m
  path: /seedboxk8s/applications/restic-backup
  prune: true
  timeout: 2m
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: topolvm
  decryption:
    provider: sops
    secretRef:
      name: sops-age
