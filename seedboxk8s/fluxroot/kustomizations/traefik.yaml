apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: traefik
  namespace: flux-system
spec:
  interval: 15m
  path: /seedboxk8s/applications/traefik
  prune: false
  timeout: 2m
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  validation: server
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: seedboxk8s-config

# TODO: should probably move this to helm for easier updates
