#!/bin/sh

if test ! -e $HOME/.config; then
    mkdir -p /psyche/dot.config
    ln -s ../../psyche/dot.config $HOME/.config
fi

if test ! -e $HOME/.ssh; then
    mkdir -p /psyche/dot.ssh
    ln -s ../../psyche/dot.ssh $HOME/.ssh
    ln -sf $HOME/.dhd/hbase/known_hosts $HOME/.ssh/known_hosts
fi
