---

capthook_user: capthook
capthook_group: capthook
capthook_webhook_vers: "2.6.11"
capthook_webhook_dl_uri: https://github.com/adnanh/webhook/releases/download/{{ capthook_webhook_vers }}/webhook-linux-{{ capthook_arch }}.tar.gz
capthook_webhook_archive_path: "{{ capthook_home }}/webhook-linux-{{ capthook_arch }}-{{ capthook_webhook_vers }}.tar.gz"
capthook_webhooks_dir: "{{ capthook_home }}/webhooks"
capthook_webhook_extracted_path: "{{ capthook_home }}/webhook-linux-{{ capthook_arch }}/webhook"
capthook_hooks_config_path: "{{ capthook_webhooks_dir }}/hooks.json"
