---
- name: restart samba
  ansible.builtin.service:
    name: "{{ item }}"
    state: restarted
  with_items:
    - smbd
    - nmbd

- name: update ca certificates
  ansible.builtin.command: update-ca-certificates
