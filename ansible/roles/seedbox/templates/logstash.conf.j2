input {
    beats {
        port => 5044
    }
    gelf {
        port => 12201
    }
}

output {
    elasticsearch {
        hosts => ["elastic:9200"]
        index => "logstash-%{+YYYY-MM-dd}"
    }
}
