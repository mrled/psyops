---
# Called once per entry in chineseroom_gitea_github_mirrors.
# Loop var: chineseroom_gitea_github_mirror

- name: Set derived facts for {{ chineseroom_gitea_github_mirror.github_repo }}
  ansible.builtin.set_fact:
    chineseroom_gitea_name: "github--{{ chineseroom_gitea_github_mirror.github_repo | replace('/', '--') }}"
    chineseroom_gitea_github_owner: "{{ chineseroom_gitea_github_mirror.github_repo.split('/')[0] }}"
    chineseroom_gitea_github_name: "{{ chineseroom_gitea_github_mirror.github_repo.split('/')[1] }}"
    chineseroom_gitea_checkout_name: "{{ chineseroom_gitea_github_mirror.checkout_name | default(chineseroom_gitea_github_mirror.github_repo.split('/')[1]) }}"

- name: Query GitHub API for {{ chineseroom_gitea_github_mirror.github_repo }}
  ansible.builtin.uri:
    url: "https://api.github.com/repos/{{ chineseroom_gitea_github_mirror.github_repo }}"
    method: GET
    headers:
      Accept: application/vnd.github+json
      Authorization: "{{ 'Bearer ' + chineseroom_gitea_github_api_token if chineseroom_gitea_github_api_token else omit }}"
  register: chineseroom_gitea_github_repo_info

- name: Set default branch fact for {{ chineseroom_gitea_github_mirror.github_repo }}
  ansible.builtin.set_fact:
    chineseroom_gitea_github_default_branch: "{{ chineseroom_gitea_github_mirror.default_branch | default(chineseroom_gitea_github_repo_info.json.default_branch) }}"

# --- Gitea repo ---

- name: Check if Gitea repo exists {{ chineseroom_gitea_name }}
  ansible.builtin.uri:
    url: "https://{{ chineseroom_gitea_domain }}/api/v1/repos/{{ chineseroom_gitea_mirror_org }}/{{ chineseroom_gitea_name }}"
    headers:
      Authorization: "token {{ chineseroom_gitea_mirror_token }}"
    method: GET
    status_code: [200, 404]
  register: chineseroom_gitea_repo_check

- name: Create Gitea repo {{ chineseroom_gitea_name }}
  ansible.builtin.uri:
    url: "https://{{ chineseroom_gitea_domain }}/api/v1/orgs/{{ chineseroom_gitea_mirror_org }}/repos"
    headers:
      Authorization: "token {{ chineseroom_gitea_mirror_token }}"
    method: POST
    body_format: json
    body:
      name: "{{ chineseroom_gitea_name }}"
      description: "Mirror of https://github.com/{{ chineseroom_gitea_github_mirror.github_repo }}"
      private: false
      auto_init: false
      default_branch: "{{ chineseroom_gitea_github_default_branch }}"
    status_code: 201
  when: chineseroom_gitea_repo_check.status == 404

# --- Gitea webhook ---

- name: Get webhooks for {{ chineseroom_gitea_name }}
  ansible.builtin.uri:
    url: "https://{{ chineseroom_gitea_domain }}/api/v1/repos/{{ chineseroom_gitea_mirror_org }}/{{ chineseroom_gitea_name }}/hooks"
    headers:
      Authorization: "token {{ chineseroom_gitea_mirror_token }}"
    method: GET
    status_code: 200
  register: chineseroom_gitea_hooks

- name: Add push webhook for {{ chineseroom_gitea_name }}
  ansible.builtin.uri:
    url: "https://{{ chineseroom_gitea_domain }}/api/v1/repos/{{ chineseroom_gitea_mirror_org }}/{{ chineseroom_gitea_name }}/hooks"
    headers:
      Authorization: "token {{ chineseroom_gitea_mirror_token }}"
    method: POST
    body_format: json
    body:
      type: gitea
      config:
        url: "http://127.0.0.1:{{ chineseroom_gitea_webhook_port }}/push-sync"
        content_type: json
      events: ["push"]
      active: true
    status_code: 201
  when: >-
    (chineseroom_gitea_hooks.json
      | selectattr('config.url', 'equalto',
          'http://127.0.0.1:' + chineseroom_gitea_webhook_port | string + '/push-sync')
      | list | length) == 0

# --- GitHub webhook ---

- name: Get GitHub webhooks for {{ chineseroom_gitea_github_mirror.github_repo }}
  ansible.builtin.uri:
    url: "https://api.github.com/repos/{{ chineseroom_gitea_github_mirror.github_repo }}/hooks"
    method: GET
    headers:
      Accept: application/vnd.github+json
      Authorization: "Bearer {{ chineseroom_gitea_github_api_token }}"
    status_code: 200
  register: chineseroom_gitea_github_hooks
  when: chineseroom_gitea_github_webhook_secret != ""

- name: Delete stale GitHub webhooks for {{ chineseroom_gitea_github_mirror.github_repo }}
  ansible.builtin.uri:
    url: "https://api.github.com/repos/{{ chineseroom_gitea_github_mirror.github_repo }}/hooks/{{ item.id }}"
    method: DELETE
    headers:
      Accept: application/vnd.github+json
      Authorization: "Bearer {{ chineseroom_gitea_github_api_token }}"
    status_code: 204
  loop: >-
    {{ chineseroom_gitea_github_hooks.json
       | selectattr('config.url', 'search', chineseroom_gitea_ts_dns_name)
       | rejectattr('config.url', 'equalto', chineseroom_gitea_ts_funnel_url + '/github-webhook')
       | list }}
  loop_control:
    label: "{{ item.config.url }}"
  when:
    - chineseroom_gitea_github_webhook_secret != ""
    - >-
      (chineseroom_gitea_github_hooks.json
        | selectattr('config.url', 'equalto',
            chineseroom_gitea_ts_funnel_url + '/github-webhook')
        | list | length) == 0

- name: Add GitHub webhook for {{ chineseroom_gitea_github_mirror.github_repo }}
  ansible.builtin.uri:
    url: "https://api.github.com/repos/{{ chineseroom_gitea_github_mirror.github_repo }}/hooks"
    method: POST
    headers:
      Accept: application/vnd.github+json
      Authorization: "Bearer {{ chineseroom_gitea_github_api_token }}"
    body_format: json
    body:
      name: web
      active: true
      events: ["push"]
      config:
        url: "{{ chineseroom_gitea_ts_funnel_url }}/github-webhook"
        content_type: json
        secret: "{{ chineseroom_gitea_github_webhook_secret }}"
        insecure_ssl: "0"
    status_code: 201
  when:
    - chineseroom_gitea_github_webhook_secret != ""
    - >-
      (chineseroom_gitea_github_hooks.json
        | selectattr('config.url', 'equalto',
            chineseroom_gitea_ts_funnel_url + '/github-webhook')
        | list | length) == 0
  no_log: true

# --- Working clone (gitea system user) ---

- name: Create working clone directory for {{ chineseroom_gitea_name }}
  ansible.builtin.file:
    path: "{{ chineseroom_gitea_mirrors_dir }}/{{ chineseroom_gitea_name }}"
    state: directory
    owner: "{{ chineseroom_gitea_user }}"
    group: "{{ chineseroom_gitea_group }}"
    mode: "0750"

- name: Initialise working clone and remotes for {{ chineseroom_gitea_name }}
  ansible.builtin.shell: |
    set -euo pipefail
    cd "{{ chineseroom_gitea_mirrors_dir }}/{{ chineseroom_gitea_name }}"
    git init -b "{{ chineseroom_gitea_github_default_branch }}"
    git lfs install
    git remote get-url origin 2>/dev/null \
      || git remote add origin \
           "http://{{ chineseroom_gitea_mirror_user }}:{{ chineseroom_gitea_mirror_token }}@127.0.0.1:{{ chineseroom_gitea_http_port }}/{{ chineseroom_gitea_mirror_org }}/{{ chineseroom_gitea_name }}.git"
    git remote get-url github 2>/dev/null \
      || git remote add github "git@github.com:{{ chineseroom_gitea_github_mirror.github_repo }}.git"
  args:
    creates: "{{ chineseroom_gitea_mirrors_dir }}/{{ chineseroom_gitea_name }}/.git"
  become: true
  become_user: "{{ chineseroom_gitea_user }}"
  no_log: true

- name: Update origin remote URL for {{ chineseroom_gitea_name }}
  community.general.ini_file:
    path: "{{ chineseroom_gitea_mirrors_dir }}/{{ chineseroom_gitea_name }}/.git/config"
    section: 'remote "origin"'
    option: url
    value: "http://{{ chineseroom_gitea_mirror_user }}:{{ chineseroom_gitea_mirror_token }}@127.0.0.1:{{ chineseroom_gitea_http_port }}/{{ chineseroom_gitea_mirror_org }}/{{ chineseroom_gitea_name }}.git"
    owner: "{{ chineseroom_gitea_user }}"
    mode: "0600"
  no_log: true

- name: Create .git/gitea metadata directory for {{ chineseroom_gitea_name }}
  ansible.builtin.file:
    path: "{{ chineseroom_gitea_mirrors_dir }}/{{ chineseroom_gitea_name }}/.git/gitea"
    state: directory
    owner: "{{ chineseroom_gitea_user }}"
    group: "{{ chineseroom_gitea_group }}"
    mode: "0750"

- name: Seed default-branch cache for {{ chineseroom_gitea_name }}
  ansible.builtin.copy:
    content: "{{ chineseroom_gitea_github_default_branch }}\n"
    dest: "{{ chineseroom_gitea_mirrors_dir }}/{{ chineseroom_gitea_name }}/.git/gitea/default-branch"
    owner: "{{ chineseroom_gitea_user }}"
    group: "{{ chineseroom_gitea_group }}"
    mode: "0640"
    force: false

- name: Create auto-push marker for {{ chineseroom_gitea_name }}
  ansible.builtin.copy:
    content: ""
    dest: "{{ chineseroom_gitea_mirrors_dir }}/{{ chineseroom_gitea_name }}/.git/gitea/auto-push"
    owner: "{{ chineseroom_gitea_user }}"
    group: "{{ chineseroom_gitea_group }}"
    mode: "0640"
    force: false

# --- Restricted user fork and checkout ---

- name: Check if fork exists for restricted user {{ chineseroom_gitea_name }}
  ansible.builtin.uri:
    url: "https://{{ chineseroom_gitea_domain }}/api/v1/repos/{{ chineseroom_restricted_user }}/{{ chineseroom_gitea_name }}"
    headers:
      Authorization: "token {{ chineseroom_gitea_admin_token }}"
    method: GET
    status_code: [200, 404]
  register: chineseroom_gitea_restricted_user_fork_check

- name: Fork repo to restricted user {{ chineseroom_gitea_name }}
  ansible.builtin.uri:
    url: |
      "https://{{ chineseroom_gitea_domain }}/api/v1/repos/{{ chineseroom_gitea_mirror_org }}/{{ chineseroom_gitea_name }}/forks?sudo={{ chineseroom_restricted_user }}"
    headers:
      Authorization: "token {{ chineseroom_gitea_admin_token }}"
    method: POST
    body_format: json
    body: {}
    status_code: 202
  when: chineseroom_gitea_restricted_user_fork_check.status == 404

- name: Clone or fetch restricted user fork {{ chineseroom_gitea_name }}
  ansible.builtin.shell: |
    set -euo pipefail
    dest="/mnt/aiworkspace/{{ chineseroom_gitea_checkout_name }}"
    if [ -d "${dest}/.git" ]; then
      git -C "${dest}" fetch --all
    else
      git clone \
        "ssh://{{ chineseroom_gitea_ssh_host }}/{{ chineseroom_restricted_user }}/{{ chineseroom_gitea_name }}.git" \
        "${dest}"
    fi
  become: true
  become_user: "{{ chineseroom_restricted_user }}"
  changed_when: false

- name: Add upstream remote to restricted user checkout of {{ chineseroom_gitea_name }}
  ansible.builtin.shell: |
    set -euo pipefail
    dest="/mnt/aiworkspace/{{ chineseroom_gitea_checkout_name }}"
    git -C "${dest}" remote get-url upstream 2>/dev/null \
      || git -C "${dest}" remote add upstream \
           "ssh://{{ chineseroom_gitea_ssh_host }}/{{ chineseroom_gitea_mirror_org }}/{{ chineseroom_gitea_name }}.git"
  become: true
  become_user: "{{ chineseroom_restricted_user }}"
  changed_when: false
