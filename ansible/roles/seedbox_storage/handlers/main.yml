---
# crypttab changes are not picked up until the initramfs is regenerated.
# cryptsetup-initramfs (installed by this role) provides the hooks that
# embed crypttab into the initramfs so disks are decrypted at boot.
- name: Update initramfs
  ansible.builtin.command: update-initramfs -u
  changed_when: true
