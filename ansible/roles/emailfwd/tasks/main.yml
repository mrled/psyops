---

- name: Install packages
  dnf:
    name:
      - mailx
      - ssmtp
    state: latest
    update_cache: yes

- name: Copy ssmtp config
  template:
    src: ssmtp.conf.j2
    dest: /etc/ssmtp/ssmtp.conf
    owner: root
    group: mail 
    mode: "0640"

