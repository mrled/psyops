version: "3"
services:

  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - "80:8080"
    volumes:
      - jenkins_home:/var/jenkins_home
    deploy:
      replicas: 1

  # backupmanager:
  #   image: alpine:latest
  #   volumes:
  #     - jenkins_home:/var/jenkins_home
  #   deploy:
  #     replicas: 1

volumes:
  jenkins_home:
